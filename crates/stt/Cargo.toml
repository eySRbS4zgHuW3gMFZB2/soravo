[package]
name = "soravo-stt"
version = "0.1.0"
edition = "2021"
license = "Proprietary"
authors = ["Soravo Engineering"]

[dependencies]
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
soravo-models = { path = "../models" }
