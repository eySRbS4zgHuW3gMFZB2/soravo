[package]
name = "soravo-models"
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["Soravo Engineering"]

[dependencies]
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"
reqwest = { version = "0.12", features = ["blocking"] }
url = "2.5"
 tempfile = "3.10"

[dev-dependencies]
mockito = "1.6"
