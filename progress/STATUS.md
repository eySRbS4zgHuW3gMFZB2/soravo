# Status

Current phase: Phase 2 — Auth/account/backend
Current task: CLOUD-009 payment service skeleton — IMPLEMENTED (`@soravo/license-api` package; server-authoritative `PaymentService.createOrder`, validated catalog, `DevPaymentProvider`, `createPaymentProvider` gate, 41 deterministic tests; ADR-024; PR not yet opened — commit in progress on `feature/cloud-009-payment-service`, do NOT merge)
Branch: feature/cloud-009-payment-service (stacked on `feature/cloud-008-session-password`, commit `d7e5284`)
Skills selected/loaded (Skill Selection Gate, 2026-09-15): loaded `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `securability-engineering`, `vitest`, `gh-cli`, `github`; `agent-security-audit` evaluated (read-only; not loaded for implementation — it targets AI-agent configuration boundaries, not this payment skeleton). `semgrep`/`codeql` CLI unavailable on host (substituted by structural invariants, migration-guard, DB/RLS assertion suites, and manual review).
Last commit on `feature/cloud-008-session-password`: `d7e5284` ("chore: record CLOUD-008 progress (PR #8 open)"); branch `feature/cloud-009-payment-service` created from that commit; working tree has uncommitted CLOUD-009 source.
Repo-gate status: `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test:supabase` 12/12 ✓ (no schema change); `pnpm build` (website + desktop) unchanged this task; `pnpm audit --prod` to be run; `cargo fmt/clippy/test` untouched (Rust not touched by CLOUD-009). Supabase security advisors show 3 pre-existing ADR-016-sanctioned WARNs (metrics functions); performance advisor unchanged (1 pre-existing INFO).
Current implementation state: CLOUD-001–008 live on `feature/cloud-006-admin-authorization` (PR #7 already merged; PR #8 open on `feature/cloud-008-session-password`, stacked on top of 006). CLOUD-009 is a new workspace package — no existing code modified except root `package.json` (gate wiring), `docs/ARCHITECTURE.md`, `supabase/README.md`, `10_ADR_INDEX.md`, and `services/license-api/README.md`:
- `services/license-api/package.json`: `@soravo/license-api` (private, type: module); scripts lint/test/typecheck/build; devDeps only.
- `services/license-api/src/payment/types.ts`: narrow `PaymentProvider` interface (`createOrder(CreateOrderRequest)`), public types (`ProductId`, `EntitlementPlan`, `Currency`, `ProviderKind`, `Product`, `ProductPrice`, `CreateOrderRequest`, `ProviderOrder`).
- `services/license-api/src/payment/errors.ts`: `PaymentError` (code/message/detail), stable code enum (`invalid_request`, `invalid_identity`, `invalid_product`, `invalid_configuration`, `provider_unavailable`, `provider_not_implemented`).
- `services/license-api/src/payment/catalog.ts`: `ProductCatalog` (validated construction), `PRODUCT_CATALOG` (USD 1200/5000 `evaluated_target`), `DEFAULT_PRODUCT_CATALOG`; `assertCatalogValid` rejects unsafe amounts, invalid currencies, plan mismatches, unknown product ids, invalid pricing statuses.
- `services/license-api/src/payment/dev-provider.ts`: `DevPaymentProvider` (success/always_fail), `DevProviderFailure`, safe sanitized provider order ids (`[A-Za-z0-9_-]`).
- `services/license-api/src/payment/provider-factory.ts`: `createPaymentProvider({ kind }, nodeEnv)` — `dev` returns fake outside production, `razorpay` → `provider_not_implemented`, unknown → `invalid_configuration`; reads no env vars.
- `services/license-api/src/payment/service.ts`: `PaymentService.createOrder(caller, input)` — UUID caller validation, catalog-resolved price, fresh UUID reference, provider orchestration mapped to safe error; prototype locked to `["constructor","createOrder"]`; no network, no database, no entitlement writes.
- `services/license-api/src/index.ts`: public surface (no `DevPaymentProvider`).
- Tests (41 tests, 5 files): `service.test.ts` (19 — A/L coverage: anonymous denied, exact initiation shape, no status/verified/paid/entitlement fields, no activation path, fresh unpredictable reference, price ignored, provider failure safe error, malformed products, admin identical behavior, safe log fields, structural no-env/no-fetch assertions), `dev-provider.test.ts` (4 — deterministic success/failure/safe ids), `provider-factory.test.ts` (6 — dev in production rejected, razorpay gate, unknown rejected, no env reads, no credential types), `catalog.test.ts` (8 — targets documented, validation rejects bad amounts/currencies/plans/statuses/unknown ids), `index.test.ts` (4 — public surface correct, no internals, e2e end-to-end).
- ADR-024 (`decisions/ADR-024-payment-service-skeleton.md`); `10_ADR_INDEX.md` updated.
- `services/license-api/README.md`: comprehensive skeleton docs replacing the Phase-11 placeholder.
- `docs/ARCHITECTURE.md` line 9 updated to describe the skeleton.
- Root `package.json`: `@soravo/license-api` wired into lint/test/typecheck; pnpm-lock.yaml refreshed.
Known failures: None; 41 package tests + 12 migration-guard tests all pass; lint clean (`--max-warnings=0`); typecheck clean.
Verification: No DB changes — no Supabase MCP / `db_assertions` / `rls_assertions` re-run required for CLOUD-009. Full repo gate chain pending (lint/typecheck/test/build/audit). Advisors pre-existing baseline noted.
Security status: server-authoritative price resolution; client-supplied amount/status/entitlement fields rejected by construction; no Razorpay credentials read in CLOUD-009; no env/secret references in `src/**` (structurally asserted); no network/SSRF surface; no entitlement activation path; factory refuses `razorpay`/dev-in-production/unknown kind; `DevPaymentProvider` never exported from public index; check-16 SECURITY DEFINER whitelist unchanged (exactly 3 functions). No audio/transcript/keystroke/clipboard/history in Supabase (unchanged).
TestSprite status: Not configured; requires dedicated account/project (human-gated). Not applicable to CLOUD-009.
Benchmark status: Not started; not applicable to CLOUD-009.
Next exact action: run full repo gate chain, commit CLOUD-009, push `feature/cloud-009-payment-service`, open PR stacked on `feature/cloud-008-session-password` — do NOT merge. Emit final report per §28.

(Last updated 2026-09-15)