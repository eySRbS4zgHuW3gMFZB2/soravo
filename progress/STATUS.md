# Status

Current phase: Phase 2 — Auth/account/backend
Current task: WEB-008 account dashboard — IMPLEMENTED (entitlements status, devices, active sessions, staged downloads, help/resource links, loading/error/empty states; existing profile/password/reauth/sign-out flows preserved; 104 website tests + 41 license-api + 12 migration-guard all green; PR not yet opened — commit pending on `feature/web-008-account-dashboard`)
Branch: feature/web-008-account-dashboard
Parent: main at `19ead8b` (CLOUD-001..009 merged via PRs #2–#9; WEB-001..007 complete). Branch created from `19ead8b`; working tree carried the WEB-008 source.
Skills selected/loaded (Skill Selection Gate, 2026-09-16): loaded `react`, `shadcn`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `vercel-react-best-practices`. Evaluated-not-loaded: `supabase-postgres-best-practices` (no DB/schema work in WEB-008), `playwright` (component tests via vitest/jsdom suffice), `agent-security-audit` (targets AI-agent configuration). `semgrep`/`codeql` CLI unavailable on host (substituted by structural invariants, migration-guard, DB/RLS assertion suites, and manual review). ASVS reference files absent on host; used the security-guidance skill index (V8.2/8.3 authz, V14.2 data protection, V16.5 error handling, V1.2 typed-client injection).
Repo-gate status: `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test` ✓ (104 website + 1 desktop + 41 license-api + 12 migration-guard), `pnpm build` ✓ (website + desktop), `pnpm audit --prod` ✓ (no known vulnerabilities), `cargo fmt --check` ✓, `cargo clippy --workspace --all-targets -D warnings` ✓, `cargo test --workspace` ✓ (Rust untouched).
Supabase state: NO schema changes in WEB-008 — no new migrations, no RLS policies, no grants. Live project `zbzhlhoxblguepplqppw` unchanged; no `db_assertions`/`rls_assertions` re-run required. Security advisors baseline unchanged.
Current implementation state (WEB-008 account dashboard):
- `apps/website/src/lib/account-service.ts` (new): `loadAccountDashboard` reads entitlements/devices/sessions using ONLY the safe granted projections (ADR-012/013) — entitlements `product, plan, status, starts_at, expires_at, updated_at`; devices `device_public_id, platform, app_version, first_seen_at, last_seen_at, revoked_at`; sessions `session_public_id, created_at, last_seen_at, revoked_at` + embedded safe device projection via PostgREST resource `devices(device_public_id, platform, app_version)`. Never selects `id`/`user_id`/`device_id`/`provider`/payment columns; row scoping is RLS (`auth.uid()`) server-side; failures surface ONE generic message (ASVS 16.5). Partial data kept when a single read fails.
- `apps/website/src/pages/account.tsx` (rewritten): signed-in dashboard now renders — account summary, loading line (`role="status"`), error banner (`role="alert"` + "Try again" retry), then Subscription and licensing / Devices / Active sessions sections with empty-state notes, staged Downloads cards (mirrors `download.tsx`, "No build yet", no fake artifacts), and Help/resources links (Downloads, Support centre, Privacy policy, Terms of service). Existing Display name / Password (+ reauthentication) forms and local/other-sessions sign-out controls preserved and always rendered (not data-gated).
- `apps/website/src/test-utils/supabase-mock.ts` (extended): `from()` now routes `entitlements`/`devices`/`sessions` with per-table data/error options, plus a `__update` mutator for the retry test; `MockSupabaseClient`/`MockAccountData` exported.
- `apps/website/src/lib/account-service.test.ts` (new, 10 tests): exact select-string assertions (no internal ids/provider/payment fields), per-read generic-error mapping, aggregation, partial-data-on-error, empty defaults.
- `apps/website/src/styles.css`: `.session-list` / `.link-list` / `.session-id` styles added.
- `apps/website/src/pages/account.test.tsx` (extended to 19 tests): empty/loaded states for entitlements/devices/sessions, error banner + retry recovery (`client.__update`), staged downloads, help links; existing reauth/sign-out/password/display-name tests unchanged and green.
Known failures: None.
Verification: All repo gates green (see above). 10 new account-service tests + 10 new account-page scenarios (≥12 WEB-008 scenarios required) + 8 preserved regression tests.
Security status: no new privileges/columns selected beyond the closed safe projections; no service-role/anon key in frontend; generic-only error rendering; no other user's data reachable (RLS is the only boundary); downloads staged (no real binaries, no false availability); no transcript/audio/keystrokes/clipboard/history in Supabase (unchanged); no DB access for pages outside signed-in state.
TestSprite status: Not configured; requires dedicated account/project (human-gated). Not applicable to this branch.
Benchmark status: Not started; not applicable to WEB-008.
Next exact action: commit WEB-008 (`feat: complete account dashboard`), push `feature/web-008-account-dashboard`, open PR vs main, verify CI green, merge (explicitly authorized for this task), then emit final report per §28 and STOP. Do NOT start WEB-009/WEB-010.

(Last updated 2026-09-16)