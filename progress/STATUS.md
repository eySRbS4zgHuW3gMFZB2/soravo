# Status

Current phase: Phase 1 — Website foundation
Current task: WEB-006 Umami integration — DONE; next is WEB-007 accessibility/performance
Branch: main (tracks origin/main)
Last commit: WEB-005 was 0be096d; WEB-006 work committed with this change set
Last successful test: 2026-09-14 — pnpm lint/typecheck/test/build (website 18 tests, desktop 1 test), pnpm audit --prod, cargo gates ALL PASS
Current implementation state: Umami website-behavior analytics integrated behind public client-safe env vars (`VITE_UMAMI_HOST_URL` + `VITE_UMAMI_WEBSITE_ID`, documented in `apps/website/.env.example`). The integration is inert by default — no tracker script loads and no events report unless both vars are set. When configured, it injects the Umami tracker (`<host>/script.js`) once with `data-website-id`, `data-host-url`, and `data-exclude-search`/`data-exclude-hash` so tracked URLs never carry query/hash content. Page views and SPA navigation are tracked automatically by the tracker (website-behavior only). Explicit custom events are limited to the site's launch-list/signup CTAs (`signup cta` with static `source` values on pricing and support). Tracked payloads contain no dictated text, audio, keystrokes, clipboard, transcripts, or raw device identifiers. Privacy-page "Website analytics boundary" wording remains accurate ("when present... page views and product-page interactions"). Host values are validated as http(s) before injection (rejects javascript: etc.); only public/client-safe values enter the bundle; no secrets.
Files changed: `apps/website/src/lib/analytics.ts` (new), `apps/website/src/main.tsx`, `apps/website/src/pages/{pricing,support}.tsx`, `apps/website/src/vite-env.d.ts`, `apps/website/.env.example` (new), `apps/website/src/analytics.test.ts` (new, 5 tests), `apps/website/src/website.test.tsx` (+1 CTA wiring test = 13)
Known failures: None. Remaining env gaps are human-gated (TestSprite account, Supabase project, Cloudflare account, signing credentials, and provisioning a real Umami host + website ID to actually ship analytics).
Security status: No secrets tracked; only client-safe `VITE_UMAMI_*` vars enter the bundle; host validated to http(s); `data-exclude-search`/`data-exclude-hash` prevent URL query/hash content entering analytics; auto-tracked payloads are standard website-behavior properties (hostname, language, referrer, screen, title, url); SRI not applied to the runtime tracker script (operator-hosted, versioned `script.js`) — recorded as open item to revisit with CSP (`script-src`) when the site is deployed; `pnpm audit --prod` clean.
TestSprite status: Not configured; requires dedicated account/project (human-gated).
Benchmark status: Not started; required before an STT production engine is selected (`12_BENCHMARK_PROTOCOL.md`).
Next exact action: WEB-007 — accessibility and performance pass on the website (semantic controls, focus/keyboard, contrast, reduced motion; note pre-existing base-ui `nativeButton` warning on `render`-overridden Buttons; CSP headers are still open for prod).