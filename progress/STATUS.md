# Status

Current phase: Phase 6 — WEB-012 Cloudflare Pages DEPLOYMENT (actual Pages project + deploy + DNS) — IN PROGRESS on `fix/web-012-deploy-workflow` (PR #17), based on `origin/main` = `7da65be` (WEB-012 config merged via PR #16, `de54e72`).
Current task: deploy the verified website production build to the Cloudflare Pages project `soravo` (`soravo.pages.dev`) and serve it at `https://soravo.xyz/` (www → apex redirect via Cloudflare), preserving Zoho MX/TXT DNS. Pages project created; infrastructure and credential config shipped; deployment execution is BLOCKED on the owner-created `CLOUDFLARE_API_TOKEN`.
Work done this session: Pages project `soravo` created (id `9d41732d-faaf-46ec-a8ea-6ae4d07461c3`, production branch `main`); deployed the build twice via MCP (direct-upload manifest, then inline multipart) — both deployments created but serve 500 everywhere because the Pages asset store rejects content uploaded via the MCP-account path (asset upload requires a JWT-authenticated `/pages/assets/upload` flow that the MCP executor cannot reach; overwriting auth is blocked by the executor proxy error 9106). Older broken deployment `1b8b4ca4` deleted; active broken deployment `7dcd34ca` stays until superseded by a real deploy. Prepared the complete deploy payload (hashes + manifest) in `/tmp/opencode/payload.json`.
Credential/infra state: GitHub variables `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` and secret `CLOUDFLARE_ACCOUNT_ID` set (client-safe publishable values only). `CLOUDFLARE_API_TOKEN` NOT set — cannot be created by automation (dashboard-only); required by both the GitHub deploy workflow and a one-shot `wrangler pages deploy`. Fixed the deploy workflow root cause (`ERR_PNPM_ADDING_TO_ROOT` from wrangler-action's `pnpm add`) by pre-installing `wrangler@4.132.0` as a root devDependency; PR #17 open.
Repo-gate status (on deployment task base = de54e72): `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test` (168 website) ✓, `pnpm build` ✓, `pnpm audit --prod` ✓, `pnpm e2e` (20/20 chromium) ✓. CI on PR #17: pending.
Skills loaded (deployment task): `cloudflare`, `cloudflare-deploy`, `wrangler`/`workers-best-practices` (probe only — no production use), `github`, `gh-cli`, `security-guidance`, `vitest`/`react` (informational). No UI, feature, or design changes; no Supabase schema/auth/Razorpay work.
(Last updated 2026-09-16)