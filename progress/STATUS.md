# Status

Current phase: Phase 2 — Auth/account/backend
Current task: CLOUD-013 admin user directory — IMPLEMENTED (server-owned immutable `profiles.public_user_id` + SECURITY DEFINER `admin_users(p_search, p_offset)` RPC, ADR-025, migration applied to dev project `zbzhlhoxblguepplqppw`, 60 db-assertions + U1–U21 rls-assertions all green live, migration-guard 12/12, commit+PR pending on `feature/cloud-admin-user-directory`)
Branch: feature/cloud-admin-user-directory
Parent: main at `6bf1a86` (WEB-009 merged via PR #11). Branch created from `6bf1a86`; tracked `origin/main`.
Skills selected/loaded (Skill Selection Gate, CLOUD-013): loaded `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `securability-engineering`, `github`, `gh-cli`, `mcp-server-review`. Evaluated-not-loaded by prior sessions: `semgrep`/`codeql` CLI unavailable on host (substituted by structural invariants, migration-guard, DB/RLS assertion suites, and manual review, as in prior tasks).
Repo-gate status: `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test` ✓ (website + desktop + 41 license-api + 12 migration-guard), `pnpm build` ✓ (website + desktop), `pnpm audit --prod` ✓ (no known vulnerabilities), `cargo fmt --check` ✓, `cargo clippy --workspace --all-targets -D warnings` ✓, `cargo test --workspace` ✓ (Rust untouched).
Supabase state: migration `20260916120000_establish_admin_user_directory.sql` APPLIED to live dev project `zbzhlhoxblguepplqppw`; live verification confirmed triggers, function signature `(p_search text, p_offset integer)`, EXECUTE granted to `authenticated` only.
Current implementation state (CLOUD-013):
- `supabase/migrations/20260916120000_establish_admin_user_directory.sql` (new, applied): `public.profiles.public_user_id` (server-owned opaque key, `text NOT NULL UNIQUE CHECK length 8..200`, value `'user-' || replace(gen_random_uuid()::text,'-','')`, backfilled before `SET NOT NULL`); `BEFORE INSERT` trigger `profiles_set_public_user_id` overwrites client values; `BEFORE UPDATE` trigger `profiles_guard_public_user_id_immutable` raises `CLOUD-013: public_user_id is immutable` for every session incl. postgres; both SECURITY INVOKER, `search_path = pg_catalog`, EXECUTE revoked from all app roles. New SECURITY DEFINER RPC `admin_users(p_search text, p_offset integer)` → `jsonb` gated FIRST in-body (`CLOUD-013: admin access required`), EXECUTE to `authenticated` only; deterministic `ORDER BY lower(u.email) COLLATE "C", u.id`; page 25 (`limit 26 + offset` + slice → `has_more`), offset 0..100000; literal case-insensitive substring search (trim, `''`→NULL, `left(…,100)`); closed 11-key per-user projection + envelope `{users, has_more, page_size, offset, search, generated_at}`.
- `decisions/ADR-025-admin-user-directory.md` (new) + `10_ADR_INDEX.md` (+1): third justified SECURITY DEFINER exception (alongside ADR-016 metrics trio).
- `supabase/tests/db_assertions.sql`: now 60 checks (whitelist = exactly four SECURITY DEFINER functions; column + trigger checks 57–60) → PASS live.
- `supabase/tests/rls_assertions.sql`: +U-block U1–U21/U21c (anon/service_role privilege denial, non-admin + claim-tampered + postgres-without-claims gate denial, admin allow with exact projection, case-insensitive search, bounded search/offset, pagination reconstruction without gaps/dupes, byte-level determinism, key-set minimization, summary exactness, account-status derivation, public_user_id ownership + immutability) → ALL GREEN live. Three latent test-file bugs fixed during the run (U8 search term `'er'`→`'lrich'`; U17 `jsonb -> key` JSON-null gotcha → `jsonb_typeof … is distinct from 'null'`; U20 RAISE placeholder mismatch `'ursula %'`); the function/migration themselves were correct and verified via targeted probes.
- `supabase/tests/migration-guard.test.mjs`: unchanged, 12/12 guarded (function EXECUTE grants not matched by grant-extraction regex).
Known failures: None.
Security status: SECURITY DEFINER WARN (advisor 0029) now covers exactly four functions (metrics trio + `admin_users`) — intentional ADR-016/025 pattern, in-body gate is the authorization boundary; EXECUTE to `authenticated` only, `anon`/`service_role` no path; `public_user_id` unguessable (UUIDv4), immutable, no internal UUIDs/provider refs/passwords/raw metadata in the projection; no new table grants/policies/writes; migration-guard + secret scan clean. Performance advisor unchanged (1 pre-existing INFO, `sessions_device_id_idx`). Semgrep/codeql unavailable on host — substituted with structural invariants, DB/RLS assertion suites, advisor checks, and manual secret scan (no credentials found).
Next exact action: commit CLOUD-013 (`feat: add admin user directory (CLOUD-013)`), push `feature/cloud-admin-user-directory`, open PR vs main, verify CI green, merge (explicitly authorized for this task), checkout `main`, pull, verify clean tree and gates, then emit the final report per §28 (requirement matrix marks the WEB-009 directory frontend wiring as the remaining gap) and STOP. Do NOT start WEB-010.
(Last updated 2026-09-16)

## Prior task record — CLOUD-001..012 / WEB-001..009

- WEB-009 admin dashboard — MERGED via PR #11 (main `6bf1a86`): owner gates, overview aggregates via the CLOUD-007 metrics RPCs, accessible zero-dep growth chart, subscription/lifetime distributions, generated-at snapshot, 123 website tests, and an honest "User directory — not yet available" note (backend was the PRD §7 gap that CLOUD-013 now closes; the frontend wiring of that directory is the next task).
- WEB-008 account dashboard — MERGED via PR #10: entitlements/devices/sessions reads via closed safe projections only, staged downloads, 104 website + 41 license-api + 12 migration-guard tests.
- CLOUD-001..009 — MERGED via PRs #2–#9: default-deny baseline, profiles/RLS, authorization hardening, entitlements, devices/sessions, admin role, product metrics (three SECURITY DEFINER RPCs), session/password flows, payment service skeleton. WEB-001..007 complete.

(Last updated 2026-09-16)