# Status

Current phase: Phase 2 — Auth/account/backend
Current task: CLOUD-005 devices/sessions authorization — COMPLETE (applied and fully verified)
Branch: feature/cloud-005-devices-sessions (stacked on `feature/cloud-004-entitlements`)
Skills selected/loaded (Skill Selection Gate, 2026-09-15): re-ran the gate for CLOUD-005; loaded `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `securability-engineering`, `vitest`, `gh-cli`, `github`. Skips recorded and unchanged: `semgrep`/`codeql` (CLI unavailable on host — substituted by structural migration-guard invariants, DB/RLS assertion suites, MCP advisors, manual review), `supply-chain-risk-auditor` (not applicable to this task), `secure-workflow-guide` (not applicable), agent security skills (no agent config change).
Last commit: TBD on `feature/cloud-005-devices-sessions` — `feat: establish devices and sessions authorization (CLOUD-005)`, pushed, PR #5 open (base `feature/cloud-004-entitlements`, stacked, no merge).
Last successful test: 2026-09-15 — migration-guard 12/12 PASS (`pnpm test:supabase`), `db_assertions.sql` (48 checks) PASS, `rls_assertions.sql` (A/B + E1–E15 + D1–D9 + S1–S10) PASS, security advisors clean (0 lints), performance advisor 1 INFO (fresh `sessions_device_id_idx`, expected). Repo-gate chain re-run and green on this branch: `pnpm lint`, `pnpm typecheck`, `pnpm test` (website 67 + desktop 1 + guard 12), `pnpm build`, `pnpm audit --prod` (no vulnerabilities), `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace` (3 passing), `cargo clippy --workspace --all-targets`.
Current implementation state: CLOUD-001/002/003/004 remain applied on dev project `soravo` (ref `zbzhlhoxblguepplqppw`). CLOUD-005 applied via Supabase MCP (recorded migration `establish_devices_and_sessions`): `public.devices` (platform CHECK, public_id UNIQUE, one-way `revoked_at`) and `public.sessions` (bound to devices via FK CASCADE, public_id UNIQUE, one-way `revoked_at`) with RLS self-owned SELECT/INSERT/UPDATE policies on both (sessions INSERT/UPDATE WITH CHECK binds only to a live owned device via EXISTS) + three SECURITY INVOKER triggers each (set_timestamps, guard_identity, guard_revocation; search_path pinned, EXECUTE revoked from app roles) + exactly `select, insert, update` grants to `authenticated`. Revocation is terminal at every layer (RLS USING `revoked_at is null`; guard_revocation rejects clearing even as postgres; revoked rows readable but immutable). Identity columns (`user_id`, public ids, `first_seen_at`/`created_at`) are immutable via UPDATE. Verified behaviorally on the live project with a self-rolled-back fixture transaction; no fixture residue.
Files changed (CLOUD-005): `supabase/migrations/20260915160000_establish_devices_and_sessions.sql` (new), `supabase/tests/db_assertions.sql` (30 → 48 checks), `supabase/tests/rls_assertions.sql` (added D1–D9 + S1–S10 scenarios, incl. S7b concurrent-sessions proof and the S10 revoked-device write-lockout, which is covered by a dedicated ACTIVE session fixture — an earlier S10 draft targeted an already-revoked row and was corrected), `supabase/tests/migration-guard.test.mjs` (11 → 12 tests: read-grant ⇄ auth.uid()-SELECT-policy pairing), `decisions/ADR-013-supabase-devices-and-sessions.md` (new), `decisions/README.md` index renumbered (Cloudflare→014, Umami→015, admin metrics→016, MCP→017, TestSprite→018, update/release→019, benchmark→020, Linux→021), `supabase/README.md` (CLOUD-005 section), plus progress files.
Known failures: None.
Security status: Cross-user isolation is structural (every policy auth.uid()-bound); sessions cannot bind to another user's or a revoked device; revocation is one-way and enforced at RLS, trigger, and postgres privilege levels; identity fields immutable via UPDATE; no DELETE grant/policy; anon and service_role hold no privileges and no RLS bypass; no `SECURITY DEFINER` in `public`; trigger `search_path` pinned; EXECUTE revoked from app roles. No secrets tracked; no service-role in any client path; no audio/transcript/keystroke/clipboard/history in Supabase. Remaining tooling gap unchanged: `semgrep`/`codeql` CLI unavailable on host (recorded limitation; substituted as above).
TestSprite status: Not configured; requires dedicated account/project (human-gated). Not applicable to CLOUD-005.
Benchmark status: Not started; not applicable to CLOUD-005.
Next exact action: deliver the CLOUD-005 final report, then HARD STOP.

(Last updated 2026-09-15)