# Status

Current phase: Phase 2 — Auth/account/backend
Current task: CLOUD-007 product metrics queries — COMPLETE AND VERIFIED (authored, gated, committed, pushed, PR #7 open; applied + verified live on dev project `zbzhlhoxblguepplqppw`)
Branch: feature/cloud-007-product-metrics (stacked on `feature/cloud-006-admin-authorization`)
Skills selected/loaded (Skill Selection Gate, 2026-09-15): loaded `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `securability-engineering`, `vitest`, `gh-cli`, `github`; plus `frontend-accessibility`/`frontend-design`/`react` reconsideration skipped (no frontend change; not needed for a DB-only task). Skips recorded and unchanged: `semgrep`/`codeql` (CLI unavailable on host — substituted by structural migration-guard invariants, DB/RLS assertion suites, MCP advisors, manual review), `supply-chain-risk-auditor` (not applicable), `secure-workflow-guide` (not applicable), agent security skills (no agent config change).
Last commit: `bc73703` (`feat: establish product metrics queries (CLOUD-007)`) + `002da77` (`chore: record CLOUD-007 progress`) on `feature/cloud-007-product-metrics`, pushed, PR #7 open (base `feature/cloud-006-admin-authorization`, stacked, no merge). Verification-results commit follows this STATUS.md update.
Repo-gate chain green on this branch: `pnpm test:supabase` 12/12, `pnpm lint`, `pnpm typecheck`, `pnpm test` (website 67 + desktop 1 + guard 12), `pnpm build`, `pnpm audit --prod` (no known vulnerabilities). Rust untouched by this task.
Current implementation state: CLOUD-001/002/003/004/005/006/007 applied on dev project `zbzhlhoxblguepplqppw`. CLOUD-007 applied as migration `establish_product_metrics` (version `20260915130217`, migration file `20260915180000_establish_product_metrics.sql`) and VERIFIED live:
- Exactly three SECURITY DEFINER functions in `public`, EXECUTE revoked from `public`/`anon`/`service_role` and granted to `authenticated` ONLY: `admin_metrics_totals()` (jsonb headline snapshot), `admin_metrics_growth(text, timestamptz default, timestamptz default)` (UTC calendar-aligned new-user series, day|week|month, half-open buckets, 10000-bucket cap), `admin_metrics_active_users(timestamptz default, timestamptz default)` (distinct non-revoked-session users in `[p_from, p_to)`).
- Every function gates FIRST on `auth.uid()` non-null + `public.profiles.role = 'admin'` (else `CLOUD-007: admin access required`), pins `search_path = pg_catalog`, schema-qualifies all refs, uses no dynamic SQL. Aggregates only — no PII/identifiers/provider refs. New index `sessions_last_seen_at_idx` serves active_users. No new tables/sequences/table grants/policies.
- ADR-016 (`decisions/ADR-016-admin-dashboard-metrics.md`) documents the SECURITY DEFINER exception to ADR-010's no-SECURITY-DEFINER rule.
Files changed (CLOUD-007): `supabase/migrations/20260915180000_establish_product_metrics.sql` (new), `decisions/ADR-016-admin-dashboard-metrics.md` (new; ADR-016 entry already present in `10_ADR_INDEX.md`), `supabase/tests/db_assertions.sql` (53 → 57 checks; check 16 is now a SECURITY DEFINER whitelist), `supabase/tests/rls_assertions.sql` (added M1–M10 scenarios + Erin/Frank/Gina fixtures), `supabase/README.md` (CLOUD-007 section), plus progress files.
Known failures: None. Verification (2026-09-15, after `opencode mcp auth supabase` restored MCP access): `db_assertions.sql` — all 57 checks PASS (empty result, no RAISE). `rls_assertions.sql` — full suite PASS: A1-A10 (authenticated), B1-B3 (anon), E1-E15 (entitlements), D1-D9 (devices), S1-S10 (sessions), R1-R12 (role), M1-M10 (metrics). Advisors: security advisor 0029 WARN flags the three metrics functions as SECURITY DEFINER callable by `authenticated` — EXPECTED, this is exactly the ADR-016-sanctioned exception, accepted by design (remediation link <https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable>). Performance advisor 0005 INFO flags `sessions_device_id_idx` on `public.sessions` as unused — pre-existing from CLOUD-005, not a CLOUD-007 regression; `sessions_last_seen_at_idx` (CLOUD-007) not flagged. No new security/performance findings introduced by CLOUD-007.
Security status: metrics read layer is reachable only by `authenticated` sessions whose stored `profiles.role = 'admin'`; anon/service_role hold no EXECUTE (anon also lacks schema USAGE); JWT claims injected via `request.jwt.claims` (incl. `role`, `user_metadata.role`) establish no authority; `search_path` pinned; functions read-only aggregates. No secrets tracked; no service-role in any client path; no audio/transcript/keystroke/clipboard/history in Supabase. Remaining tooling gap unchanged: `semgrep`/`codeql` CLI unavailable on host (recorded limitation; substituted as above).
TestSprite status: Not configured; requires dedicated account/project (human-gated). Not applicable to CLOUD-007.
Benchmark status: Not started; not applicable to CLOUD-007.
Next exact action: merge PR #7 into `feature/cloud-006-admin-authorization` (human-gated merge — ALL gates now green: repo gates, migration applied, db_assertions 57/57, rls_assertions all scenarios, advisors recorded). Deliver the CLOUD-007 final report, then HARD STOP — do not start CLOUD-008.

(Last updated 2026-09-15)