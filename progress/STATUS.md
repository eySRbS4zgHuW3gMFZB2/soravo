# Status

Current phase: Phase 2 — Auth/account/backend
Current task: CLOUD-003 RLS/authorization hardening — COMPLETE (applied and verified)
Branch: feature/cloud-003-rls-authorization (stacked on `feature/cloud-002-supabase-auth`)
Skills selected/loaded (Skill Selection Gate, 2026-09-15): re-ran the gate for CLOUD-003; loaded `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `securability-engineering`, `vitest`, `mcp-server-review`, `gh-cli`, `github`. Skips recorded and unchanged: `semgrep`/`codeql` (CLI unavailable on host — substituted by structural migration-guard invariants, DB/RLS assertion suites, MCP advisors, manual review), `supply-chain-risk-auditor` (not applicable to this task), `secure-workflow-guide` (not applicable), agent security skills (no agent config change).
Last commit: CLOUD-003 head `098c285`-based `feature/cloud-003-rls-authorization` — `feat: harden supabase rls authorization (CLOUD-003)`, pushed, PR #3 open (base `feature/cloud-002-supabase-auth`, stacked).
Last successful test: 2026-09-15 — website 67 tests / 9 files PASS, desktop 1 test PASS, migration-guard 9/9 PASS, `db_assertions.sql` (20 checks) PASS, `rls_assertions.sql` (fixture-based, scenarios A1–A10 + B1–B3) PASS, security + performance advisors both clean (0 lints). Repo-gate chain green: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod` (no vulnerabilities), `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets`.
Current implementation state: CLOUD-001 baseline and CLOUD-002 auth/profile remain applied on dev project `soravo` (ref `zbzhlhoxblguepplqppw`). CLOUD-003 applied via Supabase MCP (`20260915140000_harden_rls_authorization.sql`, recorded migration `harden_rls_authorization` 20260915094537): `anon` lost USAGE/CREATE on `public`; PUBLIC lost USAGE on `public` so the anonymous role is denied at the schema privilege layer (schema ACL now `{pg_database_owner=UC, postgres=U, authenticated=U, service_role=U}`); schema-less postgres function default revokes built-in `EXECUTE ... FROM PUBLIC`; direct EXECUTE on `public.profiles_set_timestamps()` revoked from every app role (trigger behavior verified with real fixtures — still fires). No policy, table, grant, or SECURITY DEFINER change. `profiles` and `auth.users` both back at 0 fixture rows after the self-contained test transaction rolled back.
Files changed (CLOUD-003): `supabase/migrations/20260915140000_harden_rls_authorization.sql` (new), `supabase/tests/db_assertions.sql` (16 → 20 checks), `supabase/tests/rls_assertions.sql` (fixture-based behavioral matrix + rollback cleanup), `supabase/tests/migration-guard.test.mjs` (7 → 9 tests: no broad `USING (true)`/`WITH CHECK (true)` policies, no authorization via `user_metadata`), `decisions/ADR-010-supabase-schema-and-rls.md` (CLOUD-003 addendum), `supabase/README.md` (CLOUD-003 section), plus progress files.
Known failures: None.
Security status: Anonymous access denied at three independent layers (schema USAGE, object grants, RLS). No secrets tracked; no `service_role` in any client path; no `SECURITY DEFINER` in `public`; function `search_path` pinned; no `user_metadata`-based authorization (now structurally enforced). Remaining tooling gap unchanged: `semgrep`/`codeql` CLI unavailable on host (recorded limitation; substituted as above).
TestSprite status: Not configured; requires dedicated account/project (human-gated). Not applicable to CLOUD-003.
Benchmark status: Not started; not applicable to CLOUD-003.
Next exact action: CLOUD-004 (entitlements, per `05_TASK_BREAKDOWN.md`) — deliver the CLOUD-003 22-point final report, then HARD STOP.

(Last updated 2026-09-15)