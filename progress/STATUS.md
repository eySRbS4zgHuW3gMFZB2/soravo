# Status

Current phase: Phase 2 — Auth/account/backend (frontend wire-up)
Current task: WEB-009 admin user directory frontend — IMPLEMENTED (typed `admin_users` client service + searchable/paginated directory on the owner dashboard, generic-only errors, race-guarded requests; service + page tests added; all gates green locally; commit+PR pending on `feature/web-009-admin-user-directory`)
Branch: feature/web-009-admin-user-directory
Parent: main at `08c8280` (CLOUD-013 merged via PR #12). Branch created from `origin/main` `08c8280`; tracks `origin/main`.
Skills selected/loaded (Skill Selection Gate, WEB-009): loaded `react`, `shadcn`, `frontend-accessibility`, `web-design-guidelines`, `frontend-design`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `github`, `gh-cli`, `vercel-react-best-practices`. Evaluated-not-loaded by prior sessions: `semgrep`/`codeql` CLI unavailable on host (substituted by structural invariants, migration-guard, DB/RLS assertion suites, and manual review, as in prior tasks); `supabase-postgres-best-practices` not re-loaded (no DB changes in this task).
Repo-gate status: `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test` ✓ (138 website + desktop + 41 license-api + 12 migration-guard), `pnpm build` ✓ (website + desktop). Rust untouched — no cargo gates required.
Current implementation state (WEB-009):
- `apps/website/src/lib/admin-users-service.ts` (new): typed RPC wrapper for `admin_users(p_search, p_offset)` — envelope/row types mirroring ADR-025; client-side input normalization (`normalizeDirectorySearch`: trim, empty→null, clip to 100; `normalizeDirectoryOffset`: bound 0..100_000 mirroring the server); closed 11-key per-user projection; boundary parse of each row to safe defaults; any error — including `CLOUD-013: admin access required` — and any malformed envelope collapse to ONE generic message.
- `apps/website/src/components/admin/user-directory.tsx` (new): labeled search form (`type="search"`, `maxLength=100`, submit on demand), 25-per-page card-list directory with Previous/Next pages driven by `has_more`/`offset`, loading/empty/error states announced via `role="status"`/`role="alert"`, retry button, and a request-id guard so a stale response never overwrites a newer search. Renders only the 11 documented projection keys (public_user_id, email, display_name, role, account_status, created/updated/last_sign_in_at, entitlement, devices, sessions).
- `apps/website/src/pages/admin.tsx`: replaced the "User directory — not yet available" note with `<UserDirectory client={client} />`, rendered independently of the metrics section; lede updated (never internal ids, provider references, or financial details).
- `apps/website/src/test-utils/supabase-mock.ts`: additive `admin_users` routing + `__update` (`adminDirectory` / `adminDirectoryError`); default empty page envelope.
- `apps/website/src/lib/admin-users-service.test.ts` (new, 7 tests) and `apps/website/src/pages/admin.test.tsx` (+10 cases: authz/no-leak, authorized-fields rendering with no internal/payment data, empty directory, no-match search, over-long/whitespace search normalization, search-resets-pagination, pagination with `has_more` gating, stale-response race, generic error + retry). Website total 138 tests, all green.
- `apps/website/src/styles.css`: directory/search/pagination styles with responsive 1-column search under 680px and no horizontal overflow.
Known failures: None.
Security status: the frontend renders only the server-provided closed projection and never internal UUIDs/provider refs/payment data; generic-only error rendering (denial indistinguishable from failure, ASVS 16.5); search/offset bounded client-side to the backend's limits; request-id race guard; no new dependencies; no env or secrets in `src/**`; diff scanned for secrets (none found). Authorization remains server-side — the page gates on the immutable `profiles.role` for UX only (09_SECURITY_BASELINE.md §4); every directory read re-enforces the admin check in the SECURITY DEFINER body.
Next exact action: commit WEB-009 (`feat: complete admin user directory (WEB-009)`), push `feature/web-009-admin-user-directory`, open PR vs main, verify CI (web + rust) green, merge (explicitly authorized for this task), checkout `main`, pull, verify clean tree and gates, then emit the final report per §28 and STOP. Do NOT start WEB-010.
(Last updated 2026-09-16)

## Prior task record — CLOUD-001..013 / WEB-001..009

- CLOUD-013 admin user directory — MERGED via PR #12 (main `08c8280`): server-owned immutable `profiles.public_user_id` + SECURITY DEFINER `admin_users(p_search, p_offset)` RPC (ADR-025), applied to dev project `zbzhlhoxblguepplqppw`, 60 db-assertions + U1–U21/U21c rls-assertions green live, migration-guard 12/12. Unblocked this WEB-009 frontend task.
- WEB-009 admin dashboard — MERGED via PR #11 (main `6bf1a86`): owner gates, overview aggregates via the CLOUD-007 metrics RPCs, accessible zero-dep growth chart, subscription/lifetime distributions, generated-at snapshot, 123 website tests, and an honest "User directory — not yet available" note (backend was the PRD §7 gap that CLOUD-013 closed; the directory frontend wiring is this task).
- WEB-008 account dashboard — MERGED via PR #10: entitlements/devices/sessions reads via closed safe projections only, staged downloads, 104 website + 41 license-api + 12 migration-guard tests.
- CLOUD-001..009 — MERGED via PRs #2–#9: default-deny baseline, profiles/RLS, authorization hardening, entitlements, devices/sessions, admin role, product metrics (three SECURITY DEFINER RPCs), session/password flows, payment service skeleton. WEB-001..007 complete.

(Last updated 2026-09-16)