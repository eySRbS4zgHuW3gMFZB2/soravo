# Status

Current phase: Phase 2 — Auth/account/backend
Current task: CLOUD-004 entitlements authorization — COMPLETE (applied and fully verified)
Branch: feature/cloud-004-entitlements (stacked on `feature/cloud-003-rls-authorization`)
Skills selected/loaded (Skill Selection Gate, 2026-09-15): re-ran the gate for CLOUD-004; loaded `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `securability-engineering`, `vitest`, `gh-cli`, `github`. Skips recorded and unchanged: `semgrep`/`codeql` (CLI unavailable on host — substituted by structural migration-guard invariants, DB/RLS assertion suites, MCP advisors, manual review), `supply-chain-risk-auditor` (not applicable to this task), `secure-workflow-guide` (not applicable), agent security skills (no agent config change).
Last commit: `6d1d0f2` on `feature/cloud-004-entitlements` — `feat: establish entitlement authorization (CLOUD-004)`, pushed, PR #4 open (base `feature/cloud-003-rls-authorization`, stacked, no merge).
Last successful test: 2026-09-15 — migration-guard 11/11 PASS (`pnpm test:supabase`), `db_assertions.sql` (30 checks) PASS, `rls_assertions.sql` (A1–A10 + B1–B3 + E1–E15) PASS, security + performance advisors both clean (0 lints). Repo-gate chain re-run and green on this branch: `pnpm lint`, `pnpm typecheck`, website 67 tests / 9 files PASS, desktop 1 test PASS, `pnpm build` OK, `pnpm audit --prod` (no vulnerabilities), `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace` (3 passing), `cargo clippy --workspace --all-targets`.
Current implementation state: CLOUD-001/002/003 remain applied on dev project `soravo` (ref `zbzhlhoxblguepplqppw`). CLOUD-004 applied via Supabase MCP (`20260915150000_establish_entitlements.sql`, recorded migration `establish_entitlements`): `public.entitlements` table + 6 CHECK invariants + UNIQUE(user_id, product) + RLS (`entitlements_select_own`, auth.uid()-scoped SELECT) + `entitlements_set_timestamps` trigger (SECURITY INVOKER, search_path = pg_catalog, EXECUTE revoked from app roles) + column-level SELECT grant to authenticated on the safe projection only. `id`, `user_id`, `provider`, `provider_customer_ref`, `provider_payment_ref`, and the full row (`SELECT *`) are denied to clients (empirically verified 42501); `WHERE user_id` is un-grantable; writes denied to authenticated/anon; CHECKs reject impossible states even for postgres; legitimate server transitions (renewal, monthly→lifetime, lifetime→revoked) still succeed. Verified behaviorally on live project with self-rolled-back fixture transaction; no fixture residue.
Files changed (CLOUD-004): `supabase/migrations/20260915150000_establish_entitlements.sql` (new), `supabase/tests/db_assertions.sql` (20 → 30 checks), `supabase/tests/rls_assertions.sql` (added E1–E15 entitlement scenarios), `supabase/tests/migration-guard.test.mjs` (9 → 11 tests: no broad grants to any role, write-grant policy-pairing guard), `decisions/ADR-012-supabase-entitlements-authorization.md` (new), `supabase/README.md` (CLOUD-004 section), plus progress files.
Known failures: None.
Security status: Client read path is a least-privilege column-level projection; identity/provider columns + full row unreadable; writes impossible for clients; RLS self-scope; CHECKs and UNIQUE enforce invariant entitlement states DB-side. Anonymous access denied via CLOUD-003 schema lockout + no object grants + no RLS bypass. No secrets tracked; no `service_role` in any client path; no `SECURITY DEFINER` in `public`; trigger `search_path` pinned; EXECUTE revoked from app roles. Remaining tooling gap unchanged: `semgrep`/`codeql` CLI unavailable on host (recorded limitation; substituted as above).
TestSprite status: Not configured; requires dedicated account/project (human-gated). Not applicable to CLOUD-004.
Benchmark status: Not started; not applicable to CLOUD-004.
Next exact action: deliver the CLOUD-004 final report, then HARD STOP.

(Last updated 2026-09-15)