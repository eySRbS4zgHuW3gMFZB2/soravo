# Status

Current phase: Phase 2 — Auth/account/backend
Current task: WEB-009 admin dashboard — IMPLEMENTED (owner gates, overview aggregates, accessible zero-dep growth chart, Day/Week/Month granularity, subscription/lifetime distribution, generated-at snapshot, honest user-directory gap note; existing profile role read via RLS self-select; 123 website tests green; commit pending on `feature/web-009-admin-dashboard`)
Branch: feature/web-009-admin-dashboard
Parent: main at `b9d5ac3` (CLOUD-001..009 + WEB-001..008 merged; WEB-008 via PR #10). Branch created from `b9d5ac3`; tracked `origin/main`.
Skills selected/loaded (Skill Selection Gate, WEB-009): loaded `react`, `shadcn`, `vercel-react-best-practices`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `github`, `gh-cli`. Evaluated-not-loaded: `vercel-composition-patterns` (no compound/render-prop component work; plain composition suffices), `supabase-postgres-best-practices` (no DB/schema work in WEB-009), `playwright` (component tests via vitest/jsdom suffice), `web-perf` (zero new deps; bundle stable), `supply-chain-risk-auditor` (no new deps added), `agent-security-audit`/`mcp-server-review` (target AI-agent configuration). `semgrep`/`codeql` CLI unavailable on host (substituted by structural invariants, migration-guard, and manual review, as in WEB-008).
Repo-gate status: `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test` ✓ (123 website), `pnpm build` ✓, `pnpm audit --prod` ✓ (no known vulnerabilities), `cargo fmt --check` ✓, `cargo clippy --workspace --all-targets -D warnings` ✓, `cargo test --workspace` ✓ (Rust untouched).
Supabase state: NO schema changes in WEB-009 — no new migrations, no RLS policies, no grants, no new functions. Consumes the CLOUD-007/ADR-016 metrics RPCs as-is (`admin_metrics_totals`, `admin_metrics_growth`, `admin_metrics_active_users`).
Current implementation state (WEB-009 admin dashboard):
- `apps/website/src/lib/auth-service.ts`: `Profile.role` added to the closed self-read projection (`id, display_name, role, created_at, updated_at`) in both `getProfile` and `ensureProfile`; role is server-authoritative (CLOUD-006/ADR-022), immutable by every non-superuser, and only ever read via RLS self-select (scenario R10).
- `apps/website/src/lib/admin-metrics-service.ts` (new): typed wrappers for the three metrics RPCs; ALL failures collapse to ONE generic message (`"We couldn't load owner metrics. Please try again."`, ASVS 16.5) — a non-admin or anonymous caller can never distinguish "denied" from "error"; `loadAdminMetrics` default window 30 days / unit `day`; `rpcResult` casts the awaited chain result instead of the deprecated `.returns<T>()` (avoids postgrest-js 2.116 cast sentinel).
- `apps/website/src/components/admin/charts.tsx` (new): zero-dependency accessible SVG growth bar chart (`role="img"` + descriptive aria-label + exact per-bucket values in an `sr-only` list) and stacked distribution bars whose segments carry textual counts and shares (colour is never the only carrier, WCAG 1.4.11). Charts never render identifiers — only calendar-aligned aggregates.
- `apps/website/src/pages/admin.tsx` (rewritten from stub): gate ladder — auth loading; sign-in prompt (link to /login); `user` with no profile → "Checking owner access…"; non-admin → "Owner access required." + link to /account; admin → dashboard. Metrics are fetched ONLY when the signed-in user's own stored `role === "admin"` (the RPC bodies re-enforce the check server-side, so rendering is never the authz boundary). Dashboard: Overview stat cards (registered users, paid users, active-last-30-days, registered devices incl. platform split + revoked), New user growth with Day/Week/Month `aria-pressed` selector, Monthly subscriptions + Lifetime licences distribution breakdowns, snapshot-generated timestamp (UTC), and an honest "User directory — not yet available" note (per-PRD §7 search is a backend gap — no fake UI). Loading line (`role="status"`), error banner (`role="alert"` generic message only) + Try again via `reloadToken`; data preserved across granularity switches.
- `apps/website/src/test-utils/supabase-mock.ts` (extended): additive `rpc` routing the three admin functions with per-call data/error options (`adminTotals*`, `adminGrowth*`, `adminActiveUsers*`) + `__update` support for the retry test; `profileRole` option for the created profile; existing `existingProfile` fixtures gained `role`.
- `apps/website/src/lib/admin-metrics-service.test.ts` (new, 9 tests): RPC name/arg assertions (unit + 30-day window math; custom unit/day passthrough), generic-error mapping including admin-denial (`CLOUD-007` never returned to callers), partial-data-on-error, empty defaults.
- `apps/website/src/pages/admin.test.tsx` (new, 10 tests): sign-in guard + zero RPC calls; non-admin denial + zero RPC calls; dashboard render (all overview figures); growth chart + distribution percents; accessible chart (`role="img"`, `aria-pressed` true/false); granularity refetch (`p_bucket: "month"`); pending-loading via deferred RPCs; ONE generic error + retry recovery via `client.__update` (and server denial text NOT rendered); empty window / zero distributions («—»); no user-search input, no identities (`user-1`, `alice@soravo.app`), no card/payment/cvv strings.
- `apps/website/src/styles.css`: `.stat-value`, `.filter-row`, `.chart-block`, `.chart-canvas`, `.distribution*`; uses the existing semantic chart tokens.
Known failures: None.
Security status: no new privileges/columns selected (role via the closed self-read projection only); metrics RPCs already EXECUTE-gated to `authenticated` + admin body-check (CLOUD-007, migration 20260915180000 — untouched); frontend role gate is UX-only, the RPC bodies are the authorization boundary; generic-only error rendering (denial text never leaked); no PII/user identifiers/payment/provider data rendered; no service-role/anon secret in frontend; zero new dependencies; no DB changes; no env references in `src/**`.
Next exact action: commit WEB-009 (`feat: implement admin dashboard`), push `feature/web-009-admin-dashboard`, open PR vs main, verify CI green, merge (explicitly authorized for this task), checkout `main`, pull, verify clean tree and gates, then emit the final report per §28 (requirement matrix marks the PRD §7 user-search / per-user-detail items blocked-by-backend) and STOP. Do NOT start WEB-010.

(Last updated 2026-09-16)

## Prior task record — WEB-008 account dashboard

Current phase: Phase 2 — Auth/account/backend
Current task: WEB-008 account dashboard — IMPLEMENTED (entitlements status, devices, active sessions, staged downloads, help/resource links, loading/error/empty states; existing profile/password/reauth/sign-out flows preserved; 104 website tests + 41 license-api + 12 migration-guard all green; PR not yet opened — commit pending on `feature/web-008-account-dashboard`)
Branch: feature/web-008-account-dashboard
Parent: main at `19ead8b` (CLOUD-001..009 merged via PRs #2–#9; WEB-001..007 complete). Branch created from `19ead8b`; working tree carried the WEB-008 source.
Skills selected/loaded (Skill Selection Gate, 2026-09-16): loaded `react`, `shadcn`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `vercel-react-best-practices`. Evaluated-not-loaded: `supabase-postgres-best-practices` (no DB/schema work in WEB-008), `playwright` (component tests via vitest/jsdom suffice), `agent-security-audit` (targets AI-agent configuration). `semgrep`/`codeql` CLI unavailable on host (substituted by structural invariants, migration-guard, DB/RLS assertion suites, and manual review). ASVS reference files absent on host; used the security-guidance skill index (V8.2/8.3 authz, V14.2 data protection, V16.5 error handling, V1.2 typed-client injection).
Repo-gate status: `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test` ✓ (104 website + 1 desktop + 41 license-api + 12 migration-guard), `pnpm build` ✓ (website + desktop), `pnpm audit --prod` ✓ (no known vulnerabilities), `cargo fmt --check` ✓, `cargo clippy --workspace --all-targets -D warnings` ✓, `cargo test --workspace` ✓ (Rust untouched).
Supabase state: NO schema changes in WEB-008 — no new migrations, no RLS policies, no grants. Live project `zbzhlhoxblguepplqppw` unchanged; no `db_assertions`/`rls_assertions` re-run required. Security advisors baseline unchanged.
Current implementation state (WEB-008 account dashboard):
- `apps/website/src/lib/account-service.ts` (new): `loadAccountDashboard` reads entitlements/devices/sessions using ONLY the safe granted projections (ADR-012/013) — entitlements `product, plan, status, starts_at, expires_at, updated_at`; devices `device_public_id, platform, app_version, first_seen_at, last_seen_at, revoked_at`; sessions `session_public_id, created_at, last_seen_at, revoked_at` + embedded safe device projection via PostgREST resource `devices(device_public_id, platform, app_version)`. Never selects `id`/`user_id`/`device_id`/`provider`/payment columns; row scoping is RLS (`auth.uid()`) server-side; failures surface ONE generic message (ASVS 16.5). Partial data kept when a single read fails.
- `apps/website/src/pages/account.tsx` (rewritten): signed-in dashboard now renders — account summary, loading line (`role="status"`), error banner (`role="alert"` + "Try again" retry), then Subscription and licensing / Devices / Active sessions sections with empty-state notes, staged Downloads cards (mirrors `download.tsx`, "No build yet", no fake artifacts), and Help/resources links (Downloads, Support centre, Privacy policy, Terms of service). Existing Display name / Password (+ reauthentication) forms and local/other-sessions sign-out controls preserved and always rendered (not data-gated).
- `apps/website/src/test-utils/supabase-mock.ts` (extended): `from()` now routes `entitlements`/`devices`/`sessions` with per-table data/error options, plus a `__update` mutator for the retry test; `MockSupabaseClient`/`MockAccountData` exported.
- `apps/website/src/lib/account-service.test.ts` (new, 10 tests): exact select-string assertions (no internal ids/provider/payment fields), per-read generic-error mapping, aggregation, partial-data-on-error, empty defaults.
- `apps/website/src/styles.css`: `.session-list` / `.link-list` / `.session-id` styles added.
- `apps/website/src/pages/account.test.tsx` (extended to 19 tests): empty/loaded states for entitlements/devices/sessions, error banner + retry recovery (`client.__update`), staged downloads, help links; existing reauth/sign-out/password/display-name tests unchanged and green.
Known failures: None.
Verification: All repo gates green (see above). 10 new account-service tests + 10 new account-page scenarios (≥12 WEB-008 scenarios required) + 8 preserved regression tests.
Security status: no new privileges/columns selected beyond the closed safe projections; no service-role/anon key in frontend; generic-only error rendering; no other user's data reachable (RLS is the only boundary); downloads staged (no real binaries, no false availability); no transcript/audio/keystrokes/clipboard/history in Supabase (unchanged); no DB access for pages outside signed-in state.
TestSprite status: Not configured; requires dedicated account/project (human-gated). Not applicable to this branch.
Benchmark status: Not started; not applicable to WEB-008.
Next exact action: commit WEB-008 (`feat: complete account dashboard`), push `feature/web-008-account-dashboard`, open PR vs main, verify CI green, merge (explicitly authorized for this task), then emit final report per §28 and STOP. Do NOT start WEB-009/WEB-010.

(Last updated 2026-09-16)