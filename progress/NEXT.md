# Next

Active task: **WEB-012 — Cloudflare Pages DEPLOYMENT** in progress; Pages project `soravo` exists (`soravo.pages.dev`, production branch `main`), all local gates green, deploy payload prepared. The single blocking item is the owner-created `CLOUDFLARE_API_TOKEN` (cannot be minted via API/dashboard by automation). Two deployment routes once it exists:
1. **GitHub Actions (preferred, repo-owned):** owner creates token → `gh secret set CLOUDFLARE_API_TOKEN`, merge PR #17, push/main triggers `pages-deployment.yaml` (`wrangler pages deploy apps/website/dist --project-name=soravo --branch=main`).
2. **One-shot CLI:** from repo root: `CLOUDFLARE_API_TOKEN=<token> CLOUDFLARE_ACCOUNT_ID=88e94f637378412bf8f6b540a45e8dcb pnpm exec wrangler pages deploy apps/website/dist --project-name soravo --branch main` (payload/hashes already computed in `/tmp/opencode/payload.json`). Keep the manual path only if that step must run on the host.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `04_IMPLEMENTATION_PLAN.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `11_INTERRUPTION_HANDOFF.md`, `13_RELEASE_RUNBOOK.md`, `14_ENVIRONMENT_AND_SECRETS.md`, and `decisions/ADR-014-cloudflare-hosting-and-deployment.md`.
Skills baseline: re-run the Skill Selection Gate each task. Deployment task has loaded `cloudflare`, `cloudflare-deploy`, `github`, `gh-cli`, `security-guidance`; `wrangler` loaded only for probing behavior/live docs (no production credential used). Remaining post-deploy tasks should re-load `web-perf`, `playwright`, and `semgrep`/`codeql` if available for smoke/security passes.
Inspect (resume): `apps/website/dist/` (ready 24-file build, CSP `connect-src 'self' https://zbzhlhoxblguepplqppw.supabase.co`, no 404.html/_redirects, no HSTS), `.github/workflows/pages-deployment.yaml`, root `package.json`/`pnpm-workspace.yaml` (wrangler devDep fix), `/tmp/opencode/` helpers (cfmcp.js `@file` params, cfeval.js, payload.json).
Constraints carried: `public` default-deny; SECURITY DEFINER whitelist exactly as in ADR-016+ADR-025; `profiles.role` immutable; admin authority = stored column only; no transcript/audio/keystrokes/clipboard/history in Supabase; pricing/buy remain dummy; only allowlisted client-safe `VITE_*` keys; no Razorpay domains in CSP until CLOUD-010/011; HSTS deferred; no wildcard CSP sources; no top-level `404.html`/`_redirects` in the Pages output; Zoho `soravo.xyz` MX/SPF/DKIM records must never be touched (`proxied:false`, dns-only); working-tree changes to `progress/STATUS.md` are expected (preserve, do not revert).
Remaining work after the token + deploy: (a) delete the stale active broken deployment `7dcd34ca` once superseded; (b) Phase 7 DNS/config cutover — add custom domains `soravo.xyz` + `www` to project, create proxied CNAME `soravo.xyz` → `soravo.pages.dev` and `www` → `soravo.pages.dev`, add www → apex redirect rule (ADR-014: Cloudflare-config redirect, HTTP 301, no HSTS), re-verify Zoho records (must stay `proxied:false`); (c) smoke tests on `https://soravo.xyz/` (routes, CSP/headers, no mixed content, no E2E harness) via `playwright`; (d) security pass (secret scan of dist + headers; advisors) and report; (e) update this file + STATUS.md and file the WEB-012 deployment final report (PUBLIC WEBSITE milestone).
Tests to run post-deploy: repo-standard gates unchanged; add deterministic smoke assertions against the live origin (canonical `https://soravo.xyz/` only) — 200 index, SPA fallback 200, `_headers` CSP `connect-src` includes exactly the Supabase origin, no service-role/API-token markers in served HTML/JS.
Security checks (post-deploy): served bundle graph must match source CSP; no `VITE_E2E_TEST_MODE`/Razorpay strings in served assets; no mixed content; `https://soravo.xyz/` canonical-only; HSTS still absent.
Expected completion condition: site reachable at `https://soravo.xyz/` with confirmed headers/CSP, Zoho mail DNS verified intact (dns-only), broken deployment cleaned up, PR #17 merged, all progress docs updated, final report filed with deployment status = all done for PUBLIC WEBSITE milestone.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not add further SECURITY DEFINER functions or schema changes. Do not implement or deploy Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not change DNS for mail/critical records outside the Pages cutover. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main` directly. Do not squash or force-push stacked PRs.

(Last updated 2026-09-16)