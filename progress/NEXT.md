# Next

Resume task: Phase 2 — **WEB-009 admin directory frontend wiring** per `01_PRD.md` §7 and ADR-025. CLOUD-013 (merged) delivered the backend that unblocks it: `admin_users(p_search, p_offset)` SECURITY DEFINER RPC on dev project `zbzhlhoxblguepplqppw`, closed 11-key per-user projection, 25/page with `has_more`, bounded search/offset. The admin dashboard (`apps/website/src/pages/admin.tsx`) currently renders the honest "User directory — not yet available" note; the next task wires the directory with a search box consuming this RPC (mirroring `admin-metrics-service.ts`'s typed RPC wrapper pattern and generic-only error rendering). After that PR is merged, the next task is **WEB-010** per `05_TASK_BREAKDOWN.md`. Do NOT start WEB-010 on the current branch.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md` (§7 admin), `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `decisions/ADR-025-admin-user-directory.md`, `decisions/ADR-016-admin-dashboard-metrics.md`, `decisions/ADR-022-professional-profile-role.md`, `supabase/migrations/20260916120000_establish_admin_user_directory.sql`, `supabase/tests/rls_assertions.sql` (U-block), `apps/website/src/pages/admin.tsx`, `apps/website/src/lib/admin-metrics-service.ts`, `apps/website/src/components/admin/charts.tsx`.
Skills baseline: re-run the Skill Selection Gate; base expectation `react`, `shadcn`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `vercel-react-best-practices`, `github`, `gh-cli`. `semgrep`/`codeql` CLI unavailable on host — substitute structural invariants, migration-guard, DB/RLS assertion suites, and manual review.
Inspect: the finalized page/service/components above; `apps/website/src/test-utils/supabase-mock.ts` (additive `rpc` routing + `__update` + `profileRole`); main at `6bf1a86` (CLOUD-001..013 + WEB-001..009 merged). Live DB is already migrated (`zbzhlhoxblguepplqppw`); consume `admin_users` by name + args `{p_search, p_offset}`.
Constraints carried from CLOUD-001..013 and prior WEB tasks: `public` default-deny; grants to `anon`/`authenticated` pair with RLS in the same migration (migration-guard, 12 tests); policies authorize only via `auth.uid()`; no anonymous write grants; no `service_role` in clients; SECURITY DEFINER in `public` is exactly the ADR-016+ADR-025 whitelist (four functions — do not add more without a new ADR); functions pin `search_path`; `profiles.role` immutable (CLOUD-006); admin authority = stored column only; never expose `id`/`user_id`/`device_id`/provider/payment columns (directory projection already excludes them); generic-only error rendering (no denial detail leakage); do not imply features exist that the backend cannot serve; no transcript/audio/keystrokes/clipboard/history in Supabase; no public deploy; pricing/buy remain dummy.
Exact next implementation step: on a fresh branch from `main` (`6bf1a86`+CLOUD-013 merge), add the typed `admin_users` client service + admin-page user-directory section with search box, table (public_user_id, email, display_name, role, account_status, timestamps, entitlement/device/session summaries), pagination via `has_more` + offset, loading/error/empty states, tests (service + page, incl. generic-error mapping and denial-is-error), commit, push, open a PR targeting `main`, verify CI (web + rust jobs) green, merge (explicitly authorized), checkout `main`, pull, verify clean tree and gates, then STOP and emit the final report per §28. Do not start WEB-010 on the current branch.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, `cargo fmt --check`, `cargo clippy --workspace --all-targets`, `cargo test --workspace`) plus the migration-guard suite (`pnpm test:supabase`, 12/12). No DB changes in the frontend task, so DB/RLS assertion suites do not need re-running.
Security checks: no secrets committed (scan the diff); directory RPC is admin-gated server-side; generic error rendering only; only the 11 documented projection keys rendered (no internal ids/provider/payment data); no service-role/anon key in frontend; zero new dependencies; no env references in `src/**`.
Expected completion condition: the WEB-009 directory frontend PR merged with all gates green, `main` checked out and clean, then STOP with the final report. Do not merge any PR without explicit human authorization.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not add further SECURITY DEFINER functions or schema changes (CLOUD-013's surface is final). Do not implement Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main`. Do not deploy publicly. Do not squash or force-push stacked PRs. Do not add backend/admin APIs in WEB tasks — that is a CLOUD-track task.

(Last updated 2026-09-16)