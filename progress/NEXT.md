# Next

Resume task: Phase 2 — **WEB-008** per `05_TASK_BREAKDOWN.md`. The account dashboard is implemented and fully gated (104 website tests incl. 10 new account-service tests + 10 new account-page scenarios; lint/typecheck/build/audit clean; Rust gates green). Commit and push are pending on `feature/web-008-account-dashboard`. After this PR is merged, the next task is **WEB-009 — Admin Dashboard** per `05_TASK_BREAKDOWN.md`. Do NOT start WEB-009/WEB-010 on the current branch.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `decisions/ADR-010-supabase-schema-and-rls.md`, `decisions/ADR-011-authentication-and-session-model.md`, `decisions/ADR-012-supabase-entitlements-authorization.md`, `decisions/ADR-013-supabase-devices-and-sessions.md`, `decisions/ADR-023-scoped-signout-and-password-reauthentication.md`, `apps/website/src/pages/account.tsx`, `apps/website/src/lib/account-service.ts`.
Skills to load (per the Skill Selection Gate): re-run the gate for WEB-009; base expectation `react`, `shadcn`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `vercel-react-best-practices`.
Inspect: `apps/website/src/lib/account-service.ts` (safe projections; PostgREST embed for sessions→devices); `apps/website/src/pages/account.tsx` (dashboard sections, loading/error/empty states, retry via reloadToken); `apps/website/src/test-utils/supabase-mock.ts` (`__update` mutator + per-table errors); main at `19ead8b`. Live DB unchanged in WEB-008 (`zbzhlhoxblguepplqppw`); CLOUD-001..009 provide entitlements/devices/sessions tables, RLS, column-level grants, metrics functions (ADR-016 whitelist), and GoTrue-only reauthentication/sign-out.
Constraints carried from CLOUD-001..009 and prior WEB tasks: `public` default-deny; grants to `anon`/`authenticated` pair with RLS in the same migration (migration-guard, 12 tests); policies authorize only via `auth.uid()`; no anonymous write grants; no `service_role` in clients; SECURITY DEFINER in `public` limited to the ADR-016 whitelist (three metrics functions); functions pin `search_path`; `profiles.role` immutable; admin authority = stored column only; do not expose `id`/`user_id`/`device_id`/provider/payment columns; do not imply downloads exist (staged artifacts only); no transcript/audio/keystrokes/clipboard/history in Supabase; no public deploy; pricing/buy remain dummy.
Exact next implementation step: commit WEB-008 (`feat: complete account dashboard`), push `feature/web-008-account-dashboard`, open a PR targeting `main` (note: main is now at `19ead8b` after CLOUD-009 merged — WEB-008 branch contains no CLOUD-009 carry-over, so a fast-forward or normal merge style applies), verify CI (web + rust jobs) is green on the PR, merge (explicitly authorized for WEB-008), checkout `main`, pull, verify clean tree and gates, then STOP and emit the final report per §28. Do not start WEB-009–012 on the current branch.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets`) plus the migration-guard suite. WEB-008 makes no DB changes, so the DB/RLS assertion suites do not need re-running.
Security checks: no secrets committed (scan the diff); selects limited to the closed granted projections; no service-role/anon key in frontend; generic error rendering only (no internal detail leakage); verify no false download availability; no transcript/audio/keystrokes/clipboard/history in Supabase; no new SECURITY DEFINER; no env references in `src/**`.
Expected completion condition: WEB-008 PR merged with all gates green, `main` checked out and clean, then STOP with the final report. Do not merge any PR without explicit human authorization.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not implement Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main`. Do not deploy publicly. Do not squash or force-push stacked PRs.

(Last updated 2026-09-16)