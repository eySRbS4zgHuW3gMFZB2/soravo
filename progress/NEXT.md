# Next

Resume task: Phase 2 — CLOUD-003 per `05_TASK_BREAKDOWN.md`. CLOUD-002 auth/profile is COMPLETE as of 2026-09-15 (apply + behavioral verification + advisors + gates done).
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md` (§12–13), `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `14_ENVIRONMENT_AND_SECRETS.md`, `decisions/ADR-010-supabase-schema-and-rls.md`, `decisions/ADR-011-authentication-and-session-model.md`, `supabase/README.md`.
Skills to load (per the Skill Selection Gate): re-run the gate for CLOUD-003; base expectation `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `vitest`, `securability-engineering`, `gh-cli`, `github`, plus any task-specific skills.
Inspect: current `supabase/` state (baseline + profiles + assertion suites exist); dev project `zbzhlhoxblguepplqppw` via MCP before any mutation. Repository migrations under `supabase/migrations/` are authoritative; applied to the dev project via Supabase MCP. RLS behavioral verification now uses `supabase/tests/rls_assertions.sql` (role impersonation; each scenario sets its own `request.jwt.claims` and the call must start with `set role postgres`).
Constraints carried from CLOUD-001/002: `public` is default-deny; any grant to `anon`/`authenticated` must pair with `ENABLE ROW LEVEL SECURITY` in the same migration (enforced by `migration-guard.test.mjs`); policies authorize only via `auth.uid()`, never `user_metadata`; no anonymous write grants; no `service_role` in clients; no `SECURITY DEFINER` in `public` without a later ADR; functions must pin `search_path`. Do not regress the migration discipline. On the website, auth service/context APIs are the single entry point for auth UI.
Exact next implementation step: CLOUD-003 (create as its own focused change on a fresh branch; do not reuse this commit or extend profiles RLS scope beyond the active task). If CLOUD-003 defines new tables or policies, introduce RLS together with explicit grants in one migration, then apply + verify (db_assertions + rls_assertions + advisors) exactly as CLOUD-002 did.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets`) plus the migration-guard suite and the DB/RLS assertion suites re-run via MCP.
Security checks: no secrets committed (scan the diff); new grants least-privilege and RLS-paired; advisors clean; no service-role in clients; no transcript/audio/keystrokes/clipboard/history in Supabase.
Expected completion condition: CLOUD-003 implemented, applied, verified, all gates pass, committed + pushed, PR opened, progress updated; next task = CLOUD-004.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not redesign the backend beyond the active CLOUD task. Do not run destructive migrations without human authorization. Do not modify `main`.

(Last updated 2026-09-15)