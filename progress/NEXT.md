# Next

Resume task: Phase 2 — **CLOUD-009** per `05_TASK_BREAKDOWN.md`. CLOUD-009 (payment service skeleton) is implemented and gated (41 tests pass, lint/typecheck clean, architecture docs updated). PR not yet opened: commit and push pending on `feature/cloud-009-payment-service` — do NOT merge. After the PR is merged, proceed to the next cloud task per `05_TASK_BREAKDOWN.md`.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `14_ENVIRONMENT_AND_SECRETS.md`, `decisions/ADR-010-supabase-schema-and-rls.md` (incl. CLOUD-003 addendum), `decisions/ADR-011-authentication-and-session-model.md`, `decisions/ADR-012-supabase-entitlements-authorization.md`, `decisions/ADR-013-supabase-devices-and-sessions.md`, `decisions/ADR-022-supabase-admin-role-and-authorization.md`, `decisions/ADR-023-scoped-signout-and-password-reauthentication.md`, `decisions/ADR-024-payment-service-skeleton.md`, `supabase/README.md`.
Skills to load (per the Skill Selection Gate): re-run the gate for the next CLOUD task; base expectation `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `vitest`, `gh-cli`, `github`, plus any task-specific skills.
Inspect: current `supabase/` state (foundation + profiles + entitlements + devices/sessions + admin role + product metrics exist; assertion suites at 57 DB checks / 12 guard tests / fixture-based RLS suite with A/B/E/D/S/R + M1–M10 scenarios); CLOUD-009 introduced no new SQL, so the live state on `zbzhlhoxblguepplqppw` is unchanged. Security advisors currently show 3 pre-existing ADR-016-sanctioned WARN lints; performance advisor unchanged (1 pre-existing INFO).
Constraints carried from CLOUD-001..009: `public` is default-deny; grants to `anon`/`authenticated` pair with RLS in the same migration (enforced by `migration-guard.test.mjs`, 12 tests); policies authorize only via `auth.uid()`; no anonymous write grants; no `service_role` in clients; SECURITY DEFINER in `public` is permitted ONLY as the ADR-016 whitelist (the three metrics functions) — any additional privileged function requires its own ADR and `db_assertions.sql` check-16 admission; functions must pin `search_path`; `profiles.role` immutable for non-superuser sessions; admin authority = stored column only (never JWT metadata); scoped sign-out and reauthentication flows now use GoTrue primitives exclusively and do not expand the privileged surface. CLOUD-010–012 must never regress the migration discipline or the payment skeleton's security invariants.
Exact next implementation step: open CLOUD-009 PR against `feature/cloud-008-session-password`, stack it properly, emit the final report per §28, then await merge authorization. After the PR is merged, proceed to the next cloud task per `05_TASK_BREAKDOWN.md`. Do not start CLOUD-010–012 on the current branch.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets`) plus the migration-guard suite and the extended DB/RLS assertion suites re-run via MCP when a DB mutation is involved.
Security checks: no secrets committed (scan the diff); new grants least-privilege and RLS-paired; advisor-clean; no service-role in clients; no transcript/audio/keystrokes/clipboard/history in Supabase; no new SECURITY DEFINER beyond the ADR-016 whitelist; no `RAZORPAY_*` environment variables referenced in `src/**`.
Expected completion condition: CLOUD-009 PR merged with all gates green (green check on the PR), then next CLOUD task implemented/applied/gated/committed/pushed/PR-opened/progress-updated. Do not merge any PR without explicit human authorization.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not redesign the backend beyond the active CLOUD task. Do not run destructive migrations without human authorization. Do not modify `main`. Do not implement Razorpay live integration, webhooks, or entitlement writes outside the explicit CLOUD-010/011/012 tasks. Do not squash or force-push stacked PRs.

(Last updated 2026-09-15)