# Next

Resume task: Phase 2 — start of Phase 3 planning. **WEB-010** (dashboard browser E2E, Playwright) is in progress on `feature/web-010-dashboard-e2e`. After it merges and `main` is clean, the next milestone is website deployment / Razorpay verification preparation (do NOT implement or deploy anything — plan/review only). Do not start new implementation on `main` or a stacked branch without explicit authorization.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `11_INTERRUPTION_HANDOFF.md`, `13_RELEASE_RUNBOOK.md`, `14_ENVIRONMENT_AND_SECRETS.md`.
Skills baseline: re-run the Skill Selection Gate each task. WEB-010 loaded: `playwright`, `security-guidance`, `frontend-accessibility`, `github`, `vitest`, `react`. Base expectation for web tasks otherwise `react`, `shadcn`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `vercel-react-best-practices`, `github`, `gh-cli`. `semgrep`/`codeql` CLI unavailable on host — substitute structural invariants, migration-guard, DB/RLS assertion suites, and manual review.
Inspect (resume): the WEB-010 diff on `feature/web-010-dashboard-e2e` — `apps/website/src/test-utils/e2e-fixtures.ts`, `e2e-harness.tsx`, `main.tsx`, `app.tsx`, `vite-env.d.ts`, `playwright.config.ts`, `tests/e2e/*`, `.env.example`, `.github/workflows/ci.yml`, `package.json`/lockfile, `progress/STATUS.md`, `progress/NEXT.md`.
Constraints carried from CLOUD-001..013 and prior WEB tasks: `public` default-deny; SECURITY DEFINER in `public` is exactly the ADR-016+ADR-025 whitelist (four functions — do not add more without a new ADR); `profiles.role` immutable (CLOUD-006); admin authority = stored column only; generic-only error rendering; do not imply features exist that the backend cannot serve; no transcript/audio/keystrokes/clipboard/history in Supabase; no public deploy; pricing/buy remain dummy; no service-role/anon in clients; zero new dependencies unless justified (PLAYWRIGHT IS THE JUSTIFIED WEB-010 DEPENDENCY); no env references in `src/**` (the harness is gated, dev-only, and dead-code eliminated).
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`) plus the migration-guard suite (`pnpm test:supabase`, 12/12) and the new `pnpm e2e` (after `pnpm e2e:install`). No DB changes in WEB tasks, so DB/RLS assertion suites do not need re-running.
Security checks: scan the diff for secrets; harness must be dev-only and never ship in a production bundle; generic-only error rendering asserted in E2E; no internal ids/provider/payment data rendered (WEB-009 asserts this); no service-role/anon key in frontend; no bypass of production authz (the fake client is a test double, server-side enforcement still covered by SUPABASE suites/Vitest).
Expected completion condition: WEB-010 PR merged with all gates green and `main` clean; final report filed with NEXT MILESTONE = website deployment / Razorpay verification preparation; then HARD STOP.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not add further SECURITY DEFINER functions or schema changes (CLOUD-013's surface is final). Do not implement or deploy Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not deploy the website publicly. Do not create Cloudflare infrastructure. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main`. Do not deploy publicly. Do not squash or force-push stacked PRs. Do not add backend/admin APIs in WEB tasks — that is a CLOUD-track task.

(Last updated 2026-09-16)