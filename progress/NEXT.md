# Next

Active task: **WEB-012 — Cloudflare Pages hosting/deployment configuration** in progress on `feature/web-012-cloudflare-pages-deployment` (branch exists, based on `main` at `f18f9a1`, which merged WEB-011 via PR #15). Within WEB-012: finish all gates, security review, commit, push, open PR vs `main`, wait CI, review, merge, verify `main` clean, then file the WEB-012 final report (deployment status = all NO; remaining work = the actual Cloudflare Pages project creation + DNS cutover as a separately authorized task). Do NOT create Cloudflare infrastructure, do NOT change DNS, do NOT stress-test the site, do NOT start Razorpay, during this task.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `04_IMPLEMENTATION_PLAN.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `11_INTERRUPTION_HANDOFF.md`, `13_RELEASE_RUNBOOK.md`, `14_ENVIRONMENT_AND_SECRETS.md`, and `decisions/ADR-014-cloudflare-hosting-and-deployment.md`.
Skills baseline: re-run the Skill Selection Gate each task. WEB-012 loaded: `cloudflare`, `cloudflare-deploy` (verified current Pages docs: static `_headers` applies to asset responses, SPA fallback is native when no top-level `404.html`, GitHub Actions direct upload via `cloudflare/wrangler-action@v3`), `security-guidance`, `github`, `gh-cli`, `vitest`, `react` (informational). Intentionally NOT loaded: `wrangler` (no local CLI usage, no deploy), `workers-best-practices` (removing Workers, not applicable), `playwright` (no e2e changes), `web-perf` (informational only). `semgrep`/`codeql` CLI unavailable on host — substitute structural invariants and manual review.
Inspect (resume): the WEB-012 diff — `decisions/ADR-014-cloudflare-hosting-and-deployment.md` (Pages decision; Workers + Static Assets recorded as alternative), `apps/website/scripts/prod-headers.mjs` (Pages `_headers`, verifyDist rejects 404.html/_redirects), `apps/website/vite.config.ts`, `.github/workflows/pages-deployment.yaml` (NEW), `apps/website/src/production-readiness.test.ts` (NEW Pages workflow + SPA tests), `02_TDD.md` §16, `14_ENVIRONMENT_AND_SECRETS.md`, `progress/STATUS.md`, `progress/NEXT.md`.
Constraints carried from CLOUD-001..013 and prior WEB tasks: `public` default-deny; SECURITY DEFINER in `public` is exactly the ADR-016+ADR-025 whitelist (four functions — do not add more without a new ADR); `profiles.role` immutable (CLOUD-006); admin authority = stored column only; no transcript/audio/keystrokes/clipboard/history in Supabase; no public deploy (WEB-012 is CONFIGURATION ONLY — the Pages project and DNS cutover come later, separately authorized); pricing/buy remain dummy; no service-role/anon-in-bundles violations; zero new dependencies; only allowlisted client-safe `VITE_*` keys (Supabase anon, Umami, E2E test mode); no Razorpay domains in CSP until CLOUD-010/011; HSTS deferred; no wildcard CSP sources; one authoritative Cloudflare hosting ADR (ADR-014); no top-level `404.html`/`_redirects` in the Pages output (native SPA fallback); working-tree changes to `progress/STATUS.md` are expected (preserve, do not revert).
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`) plus `pnpm e2e`; WEB-012 adds Pages-deployment tests in `apps/website/src/production-readiness.test.ts` (workflow exists + `pages deploy apps/website/dist --project-name=soravo`, secrets-only credentials, no `VITE_E2E_TEST_MODE`/Razorpay in workflow, `_headers` Pages-compatible format/line limits, no 404.html/_redirects in source) and build-time `verifyDist` asserting no `404.html`/`_redirects` in the fresh Pages output.
Security checks: generated CSP must still match the real bundle resource graph; dist must be free of E2E harness markers and Razorpay references; no credentials embedded in the workflow or repo; no HSTS preemptively; no wildcard/bare-https origins in CSP; canonical `https://soravo.xyz/` only.
Expected completion condition: WEB-012 PR merged with all gates green and `main` clean; final report filed with deployment status = all NO, and NEXT MILESTONE = ACTUAL CLOUDFLARE PAGES DEPLOYMENT (Pages project `soravo`, production branch `main`, Cloudflare zone, Namecheap cutover preserving Zoho MX/TXT) — separately authorized.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not add further SECURITY DEFINER functions or schema changes (CLOUD-013's surface is final). Do not implement or deploy Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not deploy the website or create Cloudflare infrastructure. Do not change DNS. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main` directly. Do not squash or force-push stacked PRs. Do not add backend/admin APIs in WEB tasks — that is a CLOUD-track task.

(Last updated 2026-09-16)