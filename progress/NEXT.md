# Next

Active task: **WEB-011 — deployment readiness (pre-deployment)** in progress on `feature/web-011-deployment-readiness` (branch exists, based on `main` at `feaad9b`). Within WEB-011: finish all gates, security review, commit (`feat: prepare website for production deployment`), push, open PR vs `main`, wait CI, merge, verify `main` clean. Do NOT deploy, do NOT touch Cloudflare/DNS, do NOT start Razorpay, during this task.
After WEB-011 merges: the next milestone is site deployment planning and review (do NOT implement or deploy anything without explicit authorization). Razorpay verification (`CLOUD-010`/`CLOUD-011`) remains later and must extend the CSP only when actually implemented.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `11_INTERRUPTION_HANDOFF.md`, `13_RELEASE_RUNBOOK.md`, `14_ENVIRONMENT_AND_SECRETS.md`, and `decisions/ADR-014-cloudflare-hosting-and-deployment.md`.
Skills baseline: re-run the Skill Selection Gate each task. WEB-011 loaded: `cloudflare`, `security-guidance`, `vitest`, `gh-cli`. Intentionally NOT loaded for WEB-011: `cloudflare-deploy`/`wrangler` (no deployment), `react`/`frontend-accessibility`/`frontend-design` (no UI changes), `web-perf` (informational only), `playwright` (no e2e changes). `semgrep`/`codeql` CLI unavailable on host — substitute structural invariants and manual review (WEB-011 uses deterministic header/CSP/SEO/secret tests + build-time dist verification).
Inspect (resume): the WEB-011 diff — `decisions/ADR-014-cloudflare-hosting-and-deployment.md`, `apps/website/scripts/prod-headers.mjs`, `apps/website/vite.config.ts`, `apps/website/index.html`, `apps/website/public/{robots.txt,sitemap.xml,favicon.svg}`, `apps/website/src/production-readiness.test.ts`, `progress/STATUS.md`, `progress/NEXT.md`.
Constraints carried from CLOUD-001..013 and prior WEB tasks: `public` default-deny; SECURITY DEFINER in `public` is exactly the ADR-016+ADR-025 whitelist (four functions — do not add more without a new ADR); `profiles.role` immutable (CLOUD-006); admin authority = stored column only; no transcript/audio/keystrokes/clipboard/history in Supabase; no public deploy (WEB-011 is READINESS ONLY — deployment comes later, separately authorized); pricing/buy remain dummy; no service-role/anon in bundles; zero new dependencies; only allowlisted client-safe `VITE_*` keys (Supabase anon, Umami, E2E test mode); no Razorpay domains in CSP until CLOUD-010/011; HSTS deferred; no wildcard CSP sources; working-tree changes to `progress/STATUS.md` are expected (preserve, do not revert).
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`) plus `pnpm e2e`; WEB-011 adds `apps/website/src/production-readiness.test.ts` (CSP/headers, SEO assets, allowlist/secret guardrails) and build-time dist verification in `dist/_headers` + `verifyDist`.
Security checks: generated CSP must match the real bundle resource graph (Supabase/Umami origins only when configured); dist must be free of E2E harness markers and Razorpay references; no HSTS preemptively; no wildcard/bare-https origins in CSP; canonical `https://soravo.xyz/` only; sitemap must exclude `/account`, `/admin`, `/login`, `/reset-password`.
Expected completion condition: WEB-011 PR merged with all gates green and `main` clean; final report filed with deployment status = all NO, and NEXT MILESTONE = ACTUAL CLOUDFLARE DEPLOYMENT (separately authorized).
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not add further SECURITY DEFINER functions or schema changes (CLOUD-013's surface is final). Do not implement or deploy Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not deploy the website publicly. Do not create Cloudflare infrastructure or change DNS in WEB-011. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main` directly. Do not squash or force-push stacked PRs. Do not add backend/admin APIs in WEB tasks — that is a CLOUD-track task.

(Last updated 2026-09-16)