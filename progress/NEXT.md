# Next

Resume task: Phase 2 — **WEB-009** per `05_TASK_BREAKDOWN.md`. The admin dashboard is implemented and fully gated (123 website tests incl. 9 new admin-metrics-service tests + 10 new admin-page tests; lint/typecheck/build/audit clean; Rust gates green; zero new deps; no DB changes). Commit and push are pending on `feature/web-009-admin-dashboard`. After this PR is merged, the next task is **WEB-010** per `05_TASK_BREAKDOWN.md`. Do NOT start WEB-010 on the current branch.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md` (§7 admin), `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `decisions/ADR-016-admin-dashboard-metrics.md`, `decisions/ADR-010-supabase-schema-and-rls.md`, `decisions/ADR-011-authentication-and-session-model.md`, `decisions/ADR-012-supabase-entitlements-authorization.md`, `decisions/ADR-013-supabase-devices-and-sessions.md`, `decisions/ADR-022-professional-profile-role.md`, `apps/website/src/pages/admin.tsx`, `apps/website/src/lib/admin-metrics-service.ts`, `apps/website/src/components/admin/charts.tsx`.
Skills baseline for WEB-010: re-run the Skill Selection Gate; base expectation `react`, `shadcn`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `vercel-react-best-practices` (WEB-009 additionally loaded `vercel-react-best-practices`, `github`, `gh-cli`; evaluated-not-loaded list in STATUS).
Inspect: the finalized pages/components above; `apps/website/src/test-utils/supabase-mock.ts` (additive `rpc` routing + `__update` + `profileRole`); main at `b9d5ac3` (CLOUD-001..009 + WEB-001..008 merged). Live DB unchanged in WEB-009 (`zbzhlhoxblguepplqppw`); the three metrics RPCs (CLOUD-007/ADR-016) are consumed as-is from the client by name + args.
Constraints carried from CLOUD-001..009 and prior WEB tasks: `public` default-deny; grants to `anon`/`authenticated` pair with RLS in the same migration (migration-guard, 12 tests); policies authorize only via `auth.uid()`; no anonymous write grants; no `service_role` in clients; SECURITY DEFINER in `public` limited to the ADR-016 whitelist (three metrics functions); functions pin `search_path`; `profiles.role` immutable (CLOUD-006); admin authority = stored column only; do not expose `id`/`user_id`/`device_id`/provider/payment columns; do not imply features exist that the backend cannot serve (honest "not yet available" notes); no transcript/audio/keystrokes/clipboard/history in Supabase; no public deploy; pricing/buy remain dummy; PRD §7 user-search / per-user details are BLOCKED by backend (aggregates only) — report the gap, do not fake UI or invent APIs.
Exact next implementation step: commit WEB-009 (`feat: implement admin dashboard`), push `feature/web-009-admin-dashboard`, open a PR targeting `main` (currently `b9d5ac3`), verify CI (web + rust jobs) is green on the PR, merge (explicitly authorized for WEB-009), checkout `main`, pull, verify clean tree and gates, then STOP and emit the final report per §28 (with the requirement matrix marking blocked items). Do not start WEB-010 on the current branch.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, `cargo fmt --check`, `cargo clippy --workspace --all-targets`, `cargo test --workspace`) plus the migration-guard suite. WEB-009 makes no DB changes, so the DB/RLS assertion suites do not need re-running.
Security checks: no secrets committed (scan the diff); selects limited to closed granted projections (role via self-read only); metrics RPCs already admin-gated server-side; generic error rendering only (no denial detail leakage); no PII/user identifiers/payment/provider data rendered on /admin; no service-role/anon key in frontend; zero new dependencies; no env references in `src/**`.
Expected completion condition: WEB-009 PR merged with all gates green, `main` checked out and clean, then STOP with the final report. Do not merge any PR without explicit human authorization.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not implement Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main`. Do not deploy publicly. Do not squash or force-push stacked PRs. Do not add backend/admin APIs in WEB-009/WEB-010 — that is a CLOUD-track task.

(Last updated 2026-09-16)