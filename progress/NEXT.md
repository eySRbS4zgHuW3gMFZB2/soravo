# Next

Resume task: Phase 2 — **CLOUD-008** per `05_TASK_BREAKDOWN.md`. CLOUD-007 (product metrics queries) authored + gated + committed/pushed/PR #7 as of 2026-09-15; DB apply + verification PENDING because Supabase MCP was `Unauthorized` in that session (no local CLI/Postgres available as fallback).
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `14_ENVIRONMENT_AND_SECRETS.md`, `decisions/ADR-010-supabase-schema-and-rls.md` (incl. CLOUD-003 addendum), `decisions/ADR-011-authentication-and-session-model.md`, `decisions/ADR-012-supabase-entitlements-authorization.md`, `decisions/ADR-013-supabase-devices-and-sessions.md`, `decisions/ADR-022-supabase-admin-role-and-authorization.md`, `decisions/ADR-016-admin-dashboard-metrics.md`, `supabase/README.md`.
Skills to load (per the Skill Selection Gate): re-run the gate for CLOUD-008; base expectation `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `vitest`, `gh-cli`, `github`, plus any task-specific skills.
Inspect: current `supabase/` state (foundation + profiles + authorization hardening + entitlements + devices/sessions + admin role + product metrics exist; assertion suites at 57 DB checks / 12 guard tests / fixture-based RLS suite with A/B/E/D/S/R + M1–M10 scenarios). **First restore Supabase MCP access and verify CLOUD-007 could not previously be applied/verified**: apply `20260915180000_establish_product_metrics.sql`, run `db_assertions.sql` (57) + `rls_assertions.sql` (incl. M1–M10), re-run security/performance advisors, record results, then merge PR #7 before proceeding. Dev project `zbzhlhoxblguepplqppw` via MCP before any mutation; repository migrations under `supabase/migrations/` remain authoritative.
Constraints carried from CLOUD-001..007: `public` is default-deny; grants to `anon`/`authenticated` pair with RLS in the same migration (enforced by `migration-guard.test.mjs`, 12 tests); policies authorize only via `auth.uid()`; no anonymous write grants; no `service_role` in clients; SECURITY DEFINER in `public` is permitted ONLY as the ADR-016 whitelist (the three metrics functions) — any additional privileged function requires its own ADR and db_assertions check-16 admission; functions must pin `search_path`; `profiles.role` immutable for non-superuser sessions; admin authority = stored column only (never JWT metadata). Do not regress the migration discipline.
Exact next implementation step: CLOUD-008 (create as its own focused change on a fresh branch stacked on `feature/cloud-007-product-metrics`; do not reuse prior commits or extend RLS scope beyond the active task). If CLOUD-008 defines new tables/policies, introduce RLS together with explicit grants in one migration, then apply + verify (db_assertions + rls_assertions + advisors) exactly as CLOUD-003/004/005/006 did.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets`) plus the migration-guard suite and the extended DB/RLS assertion suites re-run via MCP.
Security checks: no secrets committed (scan the diff); new grants least-privilege and RLS-paired; advisor-clean; no service-role in clients; no transcript/audio/keystrokes/clipboard/history in Supabase.
Expected completion condition: CLOUD-007 applied + verified + advisors re-run (closing the PENDING gap), then CLOUD-008 implemented, applied, verified, all gates pass, committed + pushed, PR opened, progress updated; next task = CLOUD-009-series as scheduled.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not redesign the backend beyond the active CLOUD task. Do not run destructive migrations without human authorization. Do not modify `main`. Do not perform any Razorpay / webhook / payment-provisioning work (CLOUD-009-012) outside its own future task.

(Last updated 2026-09-15)