# Next

Resume task: Phase 2 — CLOUD-004 per `05_TASK_BREAKDOWN.md`. CLOUD-003 RLS/authorization hardening is COMPLETE as of 2026-09-15 (apply + behavioral verification + advisors + gates done).
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `14_ENVIRONMENT_AND_SECRETS.md`, `decisions/ADR-010-supabase-schema-and-rls.md` (incl. the CLOUD-003 addendum), `decisions/ADR-011-authentication-and-session-model.md`, `supabase/README.md` (incl. CLOUD-003 section).
Skills to load (per the Skill Selection Gate): re-run the gate for CLOUD-004; base expectation `supabase`, `supabase-postgres-best-practices`, `security-guidance`, `vitest`, `securability-engineering`, `gh-cli`, `github`, plus any task-specific skills.
Inspect: current `supabase/` state (baseline + profiles + authorization hardening exist; assertion suites at 20 DB checks / 9 guard tests / fixture-based RLS suite); dev project `zbzhlhoxblguepplqppw` via MCP before any mutation. Repository migrations under `supabase/migrations/` are authoritative; applied to the dev project via Supabase MCP. CLOUD-003 added a privilege-layer invariant: `anon` (and PUBLIC) no longer hold USAGE on `public`, so any CLOUD-004 grant-only change must restore/retain explicit `=U` usage for the accepting roles if schema-level access is needed, and grants to `anon`/`authenticated` must pair with `ENABLE ROW LEVEL SECURITY` in the same migration.
Constraints carried from CLOUD-001/002/003: `public` is default-deny; grants to `anon`/`authenticated` pair with RLS in the same migration (enforced by `migration-guard.test.mjs`, now 9 tests incl. no broad `USING (true)`/`WITH CHECK (true)` and no `user_metadata` authorization); policies authorize only via `auth.uid()`; no anonymous write grants; no `service_role` in clients; no `SECURITY DEFINER` in `public` without a later ADR; functions must pin `search_path`; future postgres-created functions no longer inherit PUBLIC EXECUTE (verify no function regressions). Do not regress the migration discipline. On the website, auth service/context APIs are the single entry point for auth UI.
Exact next implementation step: CLOUD-004 (create as its own focused change on a fresh branch stacked on `feature/cloud-003-rls-authorization`; do not reuse this commit or extend profiles RLS scope beyond the active task). If CLOUD-004 defines new tables or policies, introduce RLS together with explicit grants in one migration, then apply + verify (db_assertions + rls_assertions + advisors) exactly as CLOUD-003 did.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets`) plus the migration-guard suite and the extended DB/RLS assertion suites re-run via MCP.
Security checks: no secrets committed (scan the diff); new grants least-privilege and RLS-paired; advisor-clean; no service-role in clients; no transcript/audio/keystrokes/clipboard/history in Supabase.
Expected completion condition: CLOUD-004 implemented, applied, verified, all gates pass, committed + pushed, PR opened, progress updated; next task = CLOUD-005.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not redesign the backend beyond the active CLOUD task. Do not run destructive migrations without human authorization. Do not modify `main`.

(Last updated 2026-09-15)