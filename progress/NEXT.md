# Next

Resume task: Phase 2 — **WEB-010** per `05_TASK_BREAKDOWN.md` after the WEB-009 directory frontend PR merges. Do NOT start WEB-010 on the current branch; only begin after this PR is merged and `main` is clean.
Read first: `progress/STATUS.md`, `README.md`, `01_PRD.md`, `02_TDD.md`, `05_TASK_BREAKDOWN.md`, `06_DOD_QA.md`, `07_AI_SKILLS.md`, `09_SECURITY_BASELINE.md`, `10_ADR_INDEX.md`, `11_INTERRUPTION_HANDOFF.md`.
Skills baseline: re-run the Skill Selection Gate each task; base expectation for web tasks `react`, `shadcn`, `frontend-design`, `web-design-guidelines`, `frontend-accessibility`, `supabase`, `vitest`, `security-guidance`, `securability-engineering`, `vercel-react-best-practices`, `github`, `gh-cli` (loaded for WEB-009). `semgrep`/`codeql` CLI unavailable on host — substitute structural invariants, migration-guard, DB/RLS assertion suites, and manual review.
Inspect: the finalized WEB-009 diff on `main` — `apps/website/src/lib/admin-users-service.ts`, `apps/website/src/components/admin/user-directory.tsx`, `apps/website/src/pages/admin.tsx`, `apps/website/src/test-utils/supabase-mock.ts`, service/page tests, `styles.css`, `progress/STATUS.md`, `progress/NEXT.md`.
Constraints carried from CLOUD-001..013 and prior WEB tasks: `public` default-deny; SECURITY DEFINER in `public` is exactly the ADR-016+ADR-025 whitelist (four functions — do not add more without a new ADR); `profiles.role` immutable (CLOUD-006); admin authority = stored column only; generic-only error rendering; do not imply features exist that the backend cannot serve; no transcript/audio/keystrokes/clipboard/history in Supabase; no public deploy; pricing/buy remain dummy; no service-role/anon in clients; zero new dependencies unless justified; no env references in `src/**`.
Tests to run: repo-standard gates (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm audit --prod`, and cargo gates if Rust is touched) plus the migration-guard suite (`pnpm test:supabase`, 12/12). No DB changes in WEB tasks, so DB/RLS assertion suites do not need re-running.
Security checks: scan the diff for secrets; generic error rendering only; no internal ids/provider/payment data rendered (WEB-009 asserts this); no service-role/anon key in frontend; no new dependencies.
Expected completion condition: the WEB-009 directory frontend PR merged with all gates green and `main` clean, then WEB-010 begins per its own task instructions. Do not merge any PR without explicit human authorization.
Do not change: Do not add cloud STT, transcript/audio storage, desktop telemetry, or Supabase Storage/Edge Functions. Do not add further SECURITY DEFINER functions or schema changes (CLOUD-013's surface is final). Do not implement Razorpay/webhooks/entitlement writes, real payment integration, or billing UI. Do not modify desktop/audio/STT surfaces. Do not run destructive migrations without human authorization. Do not modify `main`. Do not deploy publicly. Do not squash or force-push stacked PRs. Do not add backend/admin APIs in WEB tasks — that is a CLOUD-track task.

(Last updated 2026-09-16)